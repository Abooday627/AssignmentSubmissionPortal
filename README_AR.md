# بوابة تقديم الواجبات - Assignment Submission Portal

## نظرة عامة

بوابة تقديم الواجبات هي نظام ويب متكامل يسمح للطلاب بتقديم واجباتهم بسهولة، مع إرسال إشعارات تلقائية عبر Telegram للجامعات. يتضمن النظام **لوحة تحكم إدارية كاملة** لإدارة الجامعات والتخصصات والتقديمات دون الحاجة لاستخدام أوامر SQL مباشرة.

## المميزات الرئيسية

### للطلاب
- ✅ واجهة سهلة الاستخدام لتقديم الواجبات
- ✅ رفع ملفات متعددة بجميع الصيغ
- ✅ اختيار الجامعة والتخصص والمجموعة
- ✅ إشعارات فورية عند نجاح التقديم

### للإداريين
- 🎛️ **لوحة تحكم إدارية شاملة** (`/admin`)
- 🏛️ إدارة الجامعات (إضافة، تعديل، حذف)
- 🎓 إدارة التخصصات لكل جامعة
- 📊 عرض جميع التقديمات مع تفاصيلها
- 📈 إحصائيات فورية (عدد الجامعات، التخصصات، التقديمات)
- 🔔 إعدادات Telegram لكل جامعة
- 🔒 نظام صلاحيات (Admin/User)

### التقنيات
- ⚡ **Frontend**: React 19 + TypeScript + Vite
- 🚀 **Backend**: Express + tRPC + Drizzle ORM
- 💾 **Database**: MySQL
- 🎨 **UI**: Tailwind CSS + Radix UI + shadcn/ui
- 📱 **Notifications**: Telegram Bot API

## متطلبات التشغيل

- Node.js 22.x أو أحدث
- MySQL 8.0 أو أحدث
- pnpm 10.x أو أحدث

## التثبيت والإعداد

### 1. تثبيت المكتبات

```bash
pnpm install
```

### 2. إعداد قاعدة البيانات

```bash
# إنشاء قاعدة البيانات والمستخدم
mysql -u root -p

CREATE DATABASE assignment_portal;
CREATE USER 'portal_user'@'localhost' IDENTIFIED BY 'NewPortalPass2025';
GRANT ALL PRIVILEGES ON assignment_portal.* TO 'portal_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### 3. إعداد ملف البيئة (.env)

قم بإنشاء ملف `.env` في المجلد الرئيسي:

```env
DATABASE_URL=mysql://portal_user:NewPortalPass2025@localhost:3306/assignment_portal
JWT_SECRET=A_SECRET_KEY_OF_MIN_32_CHARS_FOR_JWT_TOKEN
VITE_APP_ID=YOUR_APP_ID
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im
OWNER_NAME=Administrator
OWNER_OPEN_ID=YOUR_ADMIN_OPEN_ID
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=YOUR_BACKEND_API_KEY
VITE_FRONTEND_FORGE_API_KEY=YOUR_FRONTEND_API_KEY
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im
VITE_ANALYTICS_ENDPOINT=https://analytics.manus.im
VITE_ANALYTICS_WEBSITE_ID=YOUR_ANALYTICS_ID
VITE_APP_LOGO=https://example.com/logo.png
VITE_APP_TITLE=Assignment Submission Portal
```

**ملاحظة مهمة**: 
- استبدل `YOUR_ADMIN_OPEN_ID` بـ Open ID الخاص بحسابك لتصبح Admin
- المستخدم الذي يطابق `OWNER_OPEN_ID` سيحصل على صلاحيات Admin تلقائياً

### 4. تطبيق مخطط قاعدة البيانات

```bash
pnpm db:push
```

### 5. تشغيل المشروع

#### وضع التطوير
```bash
pnpm dev
```

#### وضع الإنتاج
```bash
pnpm build
pnpm start
```

المشروع سيعمل على: `http://localhost:3000`

## الصفحات والمسارات

| المسار | الوصف | الصلاحيات |
|--------|-------|-----------|
| `/` | صفحة تقديم الواجبات للطلاب | عامة |
| `/admin` | لوحة تحكم الإدارة | Admin فقط |

## استخدام لوحة التحكم الإدارية

### الوصول إلى لوحة التحكم

1. قم بتسجيل الدخول باستخدام حساب Manus
2. تأكد من أن `openId` الخاص بك يطابق `OWNER_OPEN_ID` في ملف `.env`
3. انتقل إلى `/admin`

### إدارة الجامعات

من تبويب **Universities**:
- **إضافة جامعة جديدة**: اضغط "Add University"
  - أدخل اسم الجامعة
  - أدخل Telegram Bot Token
  - أدخل Telegram Chat ID
- **تعديل جامعة**: اضغط على أيقونة التعديل
- **حذف جامعة**: اضغط على أيقونة الحذف (سيتم حذف جميع التخصصات المرتبطة)

### إدارة التخصصات

من تبويب **Specializations**:
- **إضافة تخصص جديد**: اضغط "Add Specialization"
  - اختر الجامعة
  - أدخل اسم التخصص
- **تعديل تخصص**: اضغط على أيقونة التعديل
- **حذف تخصص**: اضغط على أيقونة الحذف

### عرض التقديمات

من تبويب **Submissions**:
- عرض جميع التقديمات مع التفاصيل الكاملة
- معلومات الطالب (الاسم، الجامعة، التخصص، المجموعة)
- تاريخ ووقت التقديم
- إمكانية حذف التقديمات

### الإحصائيات

في أعلى لوحة التحكم ستجد:
- 🏛️ عدد الجامعات
- 🎓 عدد التخصصات
- 📄 إجمالي التقديمات
- 📊 التقديمات الأخيرة (آخر 7 أيام)

## إعداد Telegram Bot

لتلقي إشعارات التقديمات عبر Telegram:

### 1. إنشاء Bot جديد

1. تحدث مع [@BotFather](https://t.me/BotFather) على Telegram
2. أرسل `/newbot`
3. اتبع التعليمات واحصل على **Bot Token**

### 2. الحصول على Chat ID

#### للمحادثات الخاصة:
1. تحدث مع [@userinfobot](https://t.me/userinfobot)
2. سيرسل لك **Chat ID** الخاص بك

#### للمجموعات أو القنوات:
1. أضف البوت إلى المجموعة/القناة
2. أرسل رسالة في المجموعة
3. افتح: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
4. ابحث عن `"chat":{"id":-1001234567890}`

### 3. إضافة الإعدادات في لوحة التحكم

1. افتح `/admin`
2. انتقل إلى تبويب **Universities**
3. عند إضافة أو تعديل جامعة، أدخل:
   - **Telegram Bot Token**: من BotFather
   - **Telegram Chat ID**: من الخطوة السابقة

## هيكل المشروع

```
AssignmentSubmissionPortal/
├── client/                          # Frontend (React)
│   └── src/
│       ├── components/
│       │   ├── admin/              # مكونات لوحة التحكم
│       │   │   ├── UniversitiesManager.tsx
│       │   │   ├── SpecializationsManager.tsx
│       │   │   └── SubmissionsViewer.tsx
│       │   └── ui/                 # مكونات UI
│       ├── pages/
│       │   ├── AdminDashboard.tsx  # لوحة التحكم الإدارية
│       │   └── SubmissionForm.tsx  # نموذج التقديم
│       └── App.tsx
├── server/                          # Backend (Express + tRPC)
│   ├── _core/                      # النظام الأساسي
│   ├── db.ts                       # دوال قاعدة البيانات
│   ├── routers.ts                  # API Routes
│   └── telegram.ts                 # تكامل Telegram
├── drizzle/                        # Database Schema
│   └── schema.ts
├── .env                            # متغيرات البيئة
├── package.json
└── README_AR.md
```

## API Routes

### Public Routes
- `submission.getUniversities` - جلب قائمة الجامعات
- `submission.getSpecializations` - جلب التخصصات حسب الجامعة
- `submission.submit` - تقديم واجب جديد

### Admin Routes (تتطلب صلاحيات Admin)
- `admin.getStatistics` - جلب الإحصائيات
- `admin.universities.*` - إدارة الجامعات
- `admin.specializations.*` - إدارة التخصصات
- `admin.submissions.*` - إدارة التقديمات

## الأمان

- ✅ التحقق من صلاحيات Admin في كل طلب API
- ✅ حماية مسارات الإدارة من الوصول غير المصرح
- ✅ تشفير كلمات المرور في قاعدة البيانات
- ✅ استخدام JWT للمصادقة
- ✅ حماية من SQL Injection عبر Drizzle ORM

## استكشاف الأخطاء

### خطأ في الاتصال بقاعدة البيانات

```bash
# تحقق من تشغيل MySQL
sudo systemctl status mysql

# تحقق من صحة بيانات الاتصال في .env
cat .env | grep DATABASE_URL
```

### لا يمكن الوصول إلى لوحة التحكم

1. تأكد من تسجيل الدخول
2. تحقق من أن `OWNER_OPEN_ID` في `.env` يطابق Open ID الخاص بك
3. افتح Console في المتصفح للتحقق من الأخطاء

### إشعارات Telegram لا تعمل

1. تحقق من صحة Bot Token و Chat ID
2. تأكد من إضافة البوت إلى المجموعة/القناة
3. تحقق من logs في Terminal

## الدعم والمساعدة

للحصول على المساعدة أو الإبلاغ عن مشاكل:
- افتح Issue على GitHub
- تواصل مع فريق التطوير

## الترخيص

MIT License

---

**تم التطوير بواسطة**: Manus AI Agent
**التاريخ**: نوفمبر 2025
**الإصدار**: 1.0.0
